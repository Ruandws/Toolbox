**Sistema:** Serviços TI - Consultor
**Data:** 2026-07-10
**Arquivo:** sistemas/servicos_ti/ui_consultor.py
**O que mudou:**
- Ajustado o tratamento do retorno da função `run_batch_automation` em `run_playwright_task`, capturando a tupla `result_msg, color` corretamente.
- Movida a atribuição da variável `color` para o bloco de execução unitária (`else`), já que a execução em lote agora retorna a cor diretamente.
**Observações:** Correção de bug no tratamento do retorno da execução em lote que falhava ao tentar iterar na tupla e ao não definir a cor corretamente.
