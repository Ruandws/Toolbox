**Sistema:** Serviços TI - Consultor
**Data:** 2026-07-10
**Arquivo:** sistemas/servicos_ti/ui_consultor.py | sistemas/servicos_ti/consultor_sti.py
**O que mudou:**
- Adicionado `show_terminal_logs` (BooleanVar + CTkSwitch) em `ui_consultor.py`; o campo é bloqueado/liberado em `_bloquear_execucao` / `_liberar_execucao` junto aos demais controles.
- Adicionada função `set_terminal_visibility` em `ui_consultor.py`: aloca console Win32 via `ctypes.AllocConsole` e redireciona `sys.stdout`/`sys.stderr` para `CONOUT$` quando o app roda sem terminal; libera o console quando o usuário desativa a opção.
- `set_terminal_visibility` agora salva `sys.stdout`/`sys.stderr` originais em `_STDOUT_BEFORE_CONSOLE`/`_STDERR_BEFORE_CONSOLE` antes de redirecionar para `CONOUT$`; ao liberar o console, restaura os streams originais, fecha os handles de `CONOUT$` e só então chama `FreeConsole`, eliminando leak de handles e risco de crash em log pós-liberação (conforme `CheckListUIPadronizada.md` §6).
- Adicionada guarda em `iniciar_execucao_individual` e `iniciar_execucao_lote`: execução é rejeitada com mensagem de erro se o modo headless estiver ativo e `show_terminal_logs` estiver desativado (conforme `CheckListUIPadronizada.md` §6 — "impedir que navegador e console fiquem ocultos ao mesmo tempo").
- `show_terminal_logs` e `set_terminal_visibility` passados como argumento para `run_automation` e `run_batch_automation` nos dois pontos de disparo.
- Parâmetro `show_terminal_logs: bool = False` adicionado às assinaturas de `run_automation` e `run_batch_automation` em `consultor_sti.py`.
- Bloco `try/except/raise` adicionado em `run_automation` e `run_batch_automation`: exceções inesperadas são logadas via `automation_logger.exception` antes de serem relançadas.
- Log `"Navegador encerrado."` movido para o bloco `finally` dos dois entry points, garantindo emissão mesmo em caso de erro.
- Log `"Relatório gerado"` e log do resumo final adicionados ao fim de `run_batch_automation`.
- Indentação de `write_report`, `summarize_batch_results`, `batch_result_color` e `return` corrigida para dentro do bloco `try` em `run_batch_automation`.
- Literal `"Usuário encontrado"` substituído pela constante `USER_FOUND_MESSAGE` no retorno `SEARCH_TYPE_FULL_NAME` de `extract_single_result`, garantindo consistência com `classify_result_status` e `format_single_result` (conforme `CheckListPreAutomacao.md` §Regras de decisão).
- Vistoria de acompanhamento: cor de status da busca unitária ainda era inferida por substring frágil (`"Nenhum" in result_msg`) em `run_playwright_task`, deixando `"Mais de um usuário encontrado"` (resultado ambíguo) colorido como verde/sucesso — mesmo risco já registrado como "Observação" nos releases de 07/07.
- `batch_result_color` renomeada para `result_color` em `consultor_sti.py` (deixou de ser exclusiva do lote) e reaproveitada por `run_automation`, que passou a retornar `Tuple[str, str]` (`mensagem, cor`) calculado a partir do `SearchResult` real, e não mais do texto formatado.
- `run_playwright_task` em `ui_consultor.py` simplificado: os dois modos (`single`/`batch`) agora desempacotam `(result_msg, color)` diretamente das funções do núcleo; substring `"Nenhum" in result_msg` removida.
**Testes:** 87 passed — `test_unit_ui_consultor`, `test_unit_consultor_sti`, `test_regression_consultor_sti`
**Ruff:** aprovado
**Observações:** —
