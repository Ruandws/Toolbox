**Sistema:** Consultor STI
**Data:** 2026-07-07
**Arquivo:** sistemas/servicos_ti/ui_consultor.py
**O que mudou:**
- `__init__`: título da janela alterado de `"Extrator - Interface Visual"` para `"Extrator - Pesquisa de Usuário"`.
- `__init__`: adicionadas `self.minsize(640, 520)` e `self.resizable(True, True)`, permitindo redimensionamento com tamanho mínimo controlado.
- `__init__`: adicionada `self.grid_rowconfigure(1, weight=1)` para que o frame de conteúdo expanda verticalmente ao redimensionar a janela.
- `__init__`: introduzido `self.frame_conteudo` do tipo `CTkScrollableFrame` como container intermediário entre a janela e `frame_inputs`; anteriormente `frame_inputs` era filho direto da janela.
- `__init__`: `frame_inputs` teve seu pai alterado de `self` para `self.frame_conteudo` e seus `padx/pady` ajustados de `(20, 10)` para `(0, 8)`.
- `__init__`: `button_run` teve seu pai alterado de `self` para `self.frame_conteudo`, recebeu `height=40` e passou a usar `sticky="e"` com padding `(12, 8)` em vez de `pady=30` centrado.
- `__init__`: `label_status` recebeu `justify="left"`, `wraplength` aumentado de 640 para 680 e posicionamento ajustado para `sticky="ew"` com `pady=(8, 18)`.
- `label_title`: padding alterado de `pady=(30, 20)` para `pady=(22, 10)`.
- `select_spreadsheet`: `filetypes` reduzido de duas entradas redundantes (`("Planilhas", "*.xlsx")` e `("Excel", "*.xlsx")`) para uma única entrada `("Excel / Planilhas", "*.xlsx")`.
- `_atualizar_visual_segmented_button`: substituição de `"segment_tipo_execucao" not in self.__dict__` por `not hasattr(...)` tentada e revertida — `hasattr` causa `RecursionError` em subclasses de `ctk.CTk` pelo `__getattr__` do tkinter; `__dict__` permanece como forma segura e correta neste contexto.

**Observações:**
- `label_batch_title` (L216–232) e `label_batch_info` (L294–312): conteúdo de texto duplicado — consolidação pendente de permissão. *(Médio)*
- `_atualizar_visual_segmented_button` (L353): acesso ao atributo privado `_buttons_dict` do `CTkSegmentedButton` — acoplamento a API interna da lib, mantido intencionalmente. Monitorar atualizações do CustomTkinter. *(Observação)*
- `run_playwright_task` (L478): cor de resultado inferida por substring `"Nenhum" in result_msg` — frágil se o retorno de `run_batch_automation` mudar. *(Observação)*
