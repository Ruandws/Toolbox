**Sistema:** Consultor STI
**Data:** 2026-07-07
**Arquivo:** sistemas/servicos_ti/ui_consultor.py | testes/servicos_ti/test_unit_ui_consultor.py
**O que mudou:**
- `class ExtratorApp` renomeada para `ConsultorSTI` em `ui_consultor.py` e na instanciação do bloco `__main__`; nome genérico substituído por identificador específico ao fluxo conforme item 1.9 do CheckListUIPadronizada.
- `self.title(...)` alterado de `"Extrator - Pesquisa de Usuário"` para `"Serviços TI - Consultor de Usuário"`; `label_title.text` alterado de `"Extrator: Pesquisa de Usuário"` para `"Serviços TI: Consultor de Usuário"`; padrão `Sistema - Ação principal` aplicado conforme item 1.10.
- `label_batch_title` removido integralmente de `create_batch_fields` — widget com texto verboso de 8 linhas descrevendo comportamento interno (zeros à esquerda, cabeçalhos aceitos, formatação XLSX); `label_batch_info` mantido com texto condensado a 3 frases objetivas: tipo de pesquisa, detecção automática de coluna e padding de CPF apenas no lote; `label_batch_title` removido de `self.widgets_lote`; rows de `label_spreadsheet`, `entry_spreadsheet`, `button_select_spreadsheet`, `label_report_dir`, `entry_report_dir`, `button_select_report_dir` e `label_batch_info` reindexadas de 7–9 para 6–8 após remoção da linha ocupada pelo label suprimido.
- `test_unit_ui_consultor.py`: todas as referências a `ui.ExtratorApp` substituídas por `ui.ConsultorSTI` na factory `criar_app_fake` e nas chamadas de método diretas dos 15 testes de UI.

**Observações:**
- `_atualizar_visual_segmented_button` (L348): acesso ao atributo privado `_buttons_dict` do `CTkSegmentedButton` mantido intencionalmente — `hasattr` provoca `RecursionError` em subclasses de `ctk.CTk`. *(Observação)*
- `run_playwright_task`: cor de resultado inferida por substring `"Nenhum" in result_msg` — frágil se retorno de `run_batch_automation` mudar. *(Observação)*
