**Sistema:** Consultor STI
**Data:** 2026-07-07
**Arquivo:** sistemas/servicos_ti/ui_consultor.py | sistemas/servicos_ti/consultor_sti.py | testes/servicos_ti/test_unit_ui_consultor.py | testes/servicos_ti/test_unit_consultor_sti.py
**O que mudou:**
- `ConsultorSTI.__init__` passou a criar `self.var_browser = BooleanVar(value=True)` e a chamar `create_execution_options()` antes do seletor de tipo de execução.
- `ConsultorSTI.create_execution_options()` foi adicionado com o checkbox `Exibir Navegador (Modo Visual)`, permitindo alternar entre execução visual e headless pela interface.
- Campos de execução unitária e em lote em `ui_consultor.py` foram reindexados no grid para acomodar a nova opção sem sobrepor o seletor `Tipo de execução`.
- `ConsultorSTI.start_automation()` passou a ler `mostrar_browser = bool(self.var_browser.get())` e repassar esse argumento nas execuções `"single"` e `"batch"`.
- `_bloquear_execucao()` e `_liberar_execucao()` passaram a desabilitar e reabilitar `self.checkbox_browser` junto com os demais controles durante a automação.
- `run_automation()` e `run_batch_automation()` em `consultor_sti.py` receberam o parâmetro opcional `mostrar_browser: bool = True` e agora usam `playwright.chromium.launch(headless=not mostrar_browser)`.
- `test_unit_ui_consultor.py` passou a simular `var_browser` e `checkbox_browser`, atualizou os argumentos esperados das threads e adicionou `test_start_automation_unitaria_propaga_modo_headless`.
- `test_unit_consultor_sti.py` adicionou fakes de Playwright e os testes `test_run_automation_usa_headless_quando_browser_oculto` e `test_run_batch_automation_usa_browser_visual_por_padrao`.

**Observações:**
- Valor padrão preservado como modo visual (`mostrar_browser=True`) para manter compatibilidade com chamadas existentes.
- `ruff check --fix` removeu imports não utilizados em `test_unit_consultor_sti.py`.
