# Nota de Atualização: Refatoração da Validação de Pesquisa (refactor)
Data: 08 de junho de 2026, às 18:18

## O que foi feito
- **Deslocamento da validação de entrada e bloqueio prévio**
  - Deslocada a validação de entrada (CPF/Nome) para ocorrer antes de iniciar o navegador na execução individual (`run_automation`).
  - Deslocada a validação de entrada para antes da chamada de `search_user()` na execução em lote (`run_batch_automation`), adicionando os casos de erro diretamente ao relatório.
  - Implementado bloqueio de CPF inválido diretamente na interface de usuário (`ui_consultor.py`) antes de iniciar a thread da automação, fornecendo feedback imediato.
  - Modificações aplicadas nos arquivos `sistemas/servicos_ti/consultor_sti.py` e `sistemas/servicos_ti/ui_consultor.py`.
