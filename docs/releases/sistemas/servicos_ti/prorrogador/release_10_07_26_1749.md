**Sistema:** servicos_ti/prorrogador
**Data:** 2026-07-10
**Arquivo:** sistemas/servicos_ti/prorrogador_sti.py | sistemas/servicos_ti/ui_prorrogador.py
**O que mudou:** Vistoria de acompanhamento das correções aplicadas no Consultor (release_10_07_26_1718.md) identificou que os mesmos dois problemas existiam no Prorrogador — a versão original de onde `set_terminal_visibility` e o resumo de lote foram copiados — sem terem recebido o fix equivalente.
- `ui_prorrogador.py`: `set_terminal_visibility` agora salva `sys.stdout`/`sys.stderr` originais em `_STDOUT_BEFORE_CONSOLE`/`_STDERR_BEFORE_CONSOLE` antes de redirecionar para `CONOUT$`; ao liberar o console, restaura os streams originais, fecha os handles de `CONOUT$` e só então chama `FreeConsole`, eliminando leak de handles e risco de crash em log pós-liberação (conforme `CheckListUIPadronizada.md` §6).
- `prorrogador_sti.py`: adicionadas as constantes `USER_NOT_FOUND_MESSAGE`, `ERROR_MESSAGE_PREFIX`, `STATUS_SUCESSO`, `STATUS_NAO_ENCONTRADO` e `STATUS_ERRO`; literal `"Usuário não Encontrado"` em `process_user_prepared_value` e os literais `"Erro: "` no loop de lote substituídos pelas constantes.
- `prorrogador_sti.py`: adicionadas `classify_batch_row_status` e `summarize_batch_results`, que quebram o resumo do lote por status (Total/Sucesso/Não encontrado/Erro), reaproveitando o mesmo padrão usado em `consultor_sti.py`.
- `run_batch_automation`: mensagem final trocou `"N usuário(s) processado(s). M linha(s) avaliadas."` pelo resumo estruturado (`resumo = summarize_batch_results(batch_rows)`), mantendo o caminho do relatório na mensagem.
- Adicionada classe `TestResumoLote` em `testes/servicos_ti/test_unit_prorrogador_sti.py` cobrindo classificação por status e o resumo agregado (vazio e com mistura de status).
**Testes:** 55 passed — `test_unit_ui_prorrogador`, `test_unit_prorrogador_sti`, `test_regression_prorrogador_sti`
**Ruff:** aprovado
**Observações:** A cor de status do Prorrogador (`run_playwright_task` em `ui_prorrogador.py`) ainda é inferida por substring (`"não encontrado" in result_msg.lower()`) — mesma fragilidade já documentada para o Consultor antes do fix desta rodada. Não corrigido aqui por estar fora do escopo dos três itens solicitados nesta vistoria; hoje não representa bug concreto porque o fluxo do Prorrogador não tem caso de "múltiplos encontrados", mas fica registrado para uma próxima vistoria. *(Observação)*
