**Sistema:** servicos_ti/prorrogador
**Data:** 2026-06-10
**Arquivos:** `sistemas/servicos_ti/prorrogador_sti.py`, `sistemas/servicos_ti/ui_prorrogador.py`
**O que mudou:**

### prorrogador_sti.py
- Adicionado `import time` (necessário para o loop de polling de login).
- Novas constantes de autenticação: `LOGIN_BUTTON_TEXT`, `LOGIN_PASSWORD_SELECTOR`, `PAGE_READY_TIMEOUT_MS`, `LOGIN_COMPLETION_TIMEOUT_MS`, `LOGIN_COMPLETION_POLL_INTERVAL_MS`, `LOGIN_FAILED_WARNING_SELECTOR`, `LOGIN_FAILED_WARNING_PATTERN`.
- Extraídas quatro funções de suporte à autenticação: `get_login_textbox_locator`, `get_login_password_locator`, `wait_for_login_page_ready`, `get_login_failed_warning_locator`, `get_visible_login_failed_warning_text`, `wait_for_login_completion`.
- `login_to_system` refatorada: `wait_until` alterado de `networkidle` → `domcontentloaded`; substituído `page.wait_for_timeout(3000)` por `wait_for_login_completion` com detecção ativa de falha.
- Indentação corrigida no bloco `process_user_prepared_value` dentro de `run_batch_automation`.

### ui_prorrogador.py
- `start_automation`: `password` capturado sem `.strip()` — preserva espaços legítimos da senha.
- Validação de campo vazio ajustada para `not password.strip()`, evitando falso positivo em senhas com espaço único enquanto bloqueia campo em branco.

