**Sistema:** servicos_ti/prorrogador
**Data:** 2026-07-21
**Arquivo:** sistemas/servicos_ti/ui_prorrogador.py
**O que mudou:** Corrige a dívida técnica documentada em `release_10_07_26_1749.md` (item "Observações").
- `run_playwright_task`: a cor de status deixou de ser inferida por substring frágil (`"não encontrado" in result_msg.lower()`) e passou a reutilizar `classify_batch_row_status(result_msg) == STATUS_NAO_ENCONTRADO`, já existente em `prorrogador_sti.py` para o resumo de lote. Elimina a duplicidade de regra de classificação de status entre o resumo de lote e a cor de status unitária, no mesmo padrão já aplicado ao Consultor (`result_color`/`classify_result_status` em `consultor_sti.py`).
- Import de `STATUS_NAO_ENCONTRADO` e `classify_batch_row_status` adicionado a partir de `prorrogador_sti`.
- Comportamento observável não muda hoje: `classify_batch_row_status` mapeia `report_message == USER_NOT_FOUND_MESSAGE` para `STATUS_NAO_ENCONTRADO`, resultado idêntico ao substring para as mensagens atuais — a diferença é robustez a novas mensagens que citem "não encontrado" sem representar o status real.
**Testes:** 55 passed — `test_unit_prorrogador_sti`, `test_regression_prorrogador_sti`, `test_unit_ui_prorrogador` (sem alteração de expectativas)
**Ruff:** aprovado
**Observações:** —
