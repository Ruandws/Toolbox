**Sistema:** servicos_ti/prorrogador
**Data:** 2026-07-06
**Arquivos:** `sistemas/servicos_ti/ui_prorrogador.py`, `testes/servicos_ti/test_unit_ui_prorrogador.py`

**O que mudou:**
- **`ui_prorrogador.py`**: Adicionados `StringVar`, `TIPO_INDIVIDUAL` e `TIPO_LOTE` para controlar explicitamente o modo de execucao da interface.
- **`ExtratorApp.create_execution_type_selector`**: Criado `CTkSegmentedButton` para selecionar entre execucao unitaria e execucao em lote, substituindo a inferencia anterior baseada no preenchimento de planilha ou pasta de relatorio.
- **`ExtratorApp._atualizar_tipo_execucao`**: Adicionada alternancia de visibilidade entre `widgets_individuais` e `widgets_lote`, exibindo apenas os campos relevantes para o modo escolhido.
- **`ExtratorApp.start_automation`**: Alterado o fluxo de decisao para usar `self.var_tipo_execucao.get()`, com mensagens separadas para ausencia de usuario alvo ou ausencia de planilha/pasta de relatorio.
- **`ExtratorApp.start_automation`**: Mantida a validacao de senha com `password.strip()` para rejeitar senha vazia ou composta apenas por espacos sem alterar o valor original enviado ao backend.
- **`ExtratorApp._bloquear_execucao` e `ExtratorApp._liberar_execucao`**: Adicionado bloqueio e liberacao dos campos e botoes durante a automacao, incluindo seletor de tipo, credenciais, data, entradas de usuario, entradas de lote e switch de terminal.
- **`threading.Thread`**: Passou a receber `daemon=True` diretamente na construcao da thread, mantendo o comportamento anterior de thread daemon.
- **`test_unit_ui_prorrogador.py`**: Criados testes unitarios da UI para alternancia entre execucao unitaria e lote, bloqueio/liberacao de controles, rejeicao de senha apenas com espacos, despacho unitario e despacho em lote pelo tipo explicito.

**Observacoes:** Testes de UI usam fakes e nao abrem janela real do CustomTkinter.
