**Sistema:** servicos_ti/prorrogador
**Data:** 2026-06-11
**Arquivos:** `sistemas/servicos_ti/prorrogador_sti.py`
**O que mudou:** Refatoração em `prorrogador_sti.py` para abstrair as ações da tela do usuário em funções menores e coesas, que serão controladas por função "maestro" - além de remover lógicas redundantes nas esperas.

### prorrogador_sti.py

#### Novas funções e renomeações
- `open_user_page`: Função extraída para centralizar a navegação à página do usuário.
- `fill_expiration_date`: Função extraída para encapsular os passos de preenchimento do input de data (click, escape, select all, type).
- `get_user_date_input_locator` renomeada para `get_expiration_date_locator`.
- `get_user_update_button_locator` renomeada para `get_update_button_locator`.
- `click_update_and_wait_for_post_save_state` renomeada para `click_update_button`.

#### Remoção de código redundante e simplificação de fluxo
- Remoção da redundância de espera na função `wait_for_user_form_ready` (e sua chamada principal), uma vez que `fill_expiration_date` e as outras funções já garantem a visibilidade de seus próprios elementos.
- A função principal `process_user_prepared_value` foi refatorada e limpa. Toda a lógica explícita de locators e inputs foi extraída para chamadas semânticas (`open_user_page`, `fill_expiration_date`, `click_update_button`), tornando o fluxo da automação declarativo e mais fácil de dar manutenção.
