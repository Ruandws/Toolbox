**Sistema:** aghu
**Data:** 2026-06-30
**Arquivo:** sistemas/aghu/cadastro_pessoa_aghu.py
**O que mudou:**
- Adicionado o dicionário `ALIASES_NATURALIDADE_AGHU` para mapear variações de escrita (ex: "brasilia df") para "Brasília".
- Adicionadas as funções `_chave_alias_naturalidade` e `normalizar_naturalidade` para tratar a conversão segura removendo acentos e caracteres não-alfanuméricos.
- Inclusão da normalização de `naturalidade` na função `normalizar_entrada`.
**Observações:**
- Redundância lógica observada nas funções `normalizar_naturalidade` e `_chave_alias_naturalidade`: ambas chamam `texto_planilha()`, de modo que o valor sofre processamento sobreposto. Sugere-se para o futuro tipar o parâmetro de `_chave_alias_naturalidade` como `(texto: str)` e realizar a chamada a `texto_planilha()` apenas uma vez.
