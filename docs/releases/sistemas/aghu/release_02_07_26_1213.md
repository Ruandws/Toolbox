**Sistema:** aghu
**Data:** 2026-07-02
**Arquivo:** sistemas/aghu/concessor_aghu.py; sistemas/aghu/ui_concessor.py
**O que mudou:**
- `concessor_aghu.py` adicionou `ALIASES_COLUNAS_PLANILHA`, `CAMPOS_CONCESSAO_OBRIGATORIOS`, `_normalizar_cabecalho_planilha`, `_mapear_colunas_planilha` e `ler_planilha_concessoes` para carregar concessoes por planilha `.xlsx` com aliases normalizados de login, protocolo, escopo e categoria.
- `concessor_aghu.py` adicionou `resultado_ignorado`, `_preparar_concessoes_lote`, `executar_concessoes_perfis` e `executar_concessao_lote` para prevalidar linhas, ignorar entradas invalidas, evitar Playwright quando nao houver perfis automatizaveis e salvar relatorio consolidado.
- `concessor_aghu.py` adicionou `processar_concessoes` para reutilizar a sessao autenticada e processar uma lista de `ConcessaoPreparada` no mesmo fluxo de navegador.
- `ui_concessor.py` adicionou `TIPO_INDIVIDUAL`, `TIPO_LOTE`, `caminho_relatorio_padrao`, seletor segmentado de tipo de execucao, campos de planilha/relatorio e seletores de arquivo para concessao em lote.
- `ui_concessor.py` alterou `iniciar_execucao` para despachar entre fluxo unitario e lote, adicionou `iniciar_execucao_lote`, `_executar_lote_thread` e `_resumir_resultados`, e passou a bloquear/liberar tambem controles de lote durante execucao.
**Observacoes:** Achados conhecidos: o lote pode reutilizar `page`/`janela_sistema` obsoletos apos retry/troca de aba em uma concessao, a UI marca lote como verde mesmo quando houver `conferir_manual`, e a cobertura automatizada ainda nao exercita lote misto com retry entre linhas. Tenho ciencia desses pontos e manteremos por hora.
