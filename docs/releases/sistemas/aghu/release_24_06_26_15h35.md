**Sistema:** aghu
**Data:** 2026-06-24
**Arquivo:** sistemas/aghu/Habilitar_impressora_em_computador/PrinterAGHU.py
**O que mudou:**
- Extraídas duas funções auxiliares `_fechar_page_silenciosamente` e `_fechar_abas_contexto` para isolar o fechamento seguro de abas do Playwright, substituindo o bloco inline `try: page.close() / except` que existia em `trocar_aba_aghux`.
- Adicionadas constantes `MAX_TENTATIVAS_AUTENTICAR_NOVA_ABA` (3) e `INTERVALO_RETRY_AUTENTICAR_NOVA_ABA_SEGUNDOS` (1) no cabeçalho do módulo.
- Refatorada `trocar_aba_aghux` com loop de retry com até N tentativas de autenticação (parâmetro `max_tentativas_autenticacao`), fechamento de todas as abas residuais antes e após cada tentativa, navegação com `wait_until="domcontentloaded"` e `timeout=15000`, e `RuntimeError` explícito após esgotar tentativas (antes falhava com `exigir_login_valido` sem mensagem de contexto).
- Adicionado fechamento de abas residuais via `_fechar_abas_contexto(context, exceto=nova_page)` após autenticação bem-sucedida, evitando vazamento de abas órfãs.
**Observações:** Não existem testes automatizados para este procedimento em `C:\Extrator2\testes`. Lacuna registrada.
