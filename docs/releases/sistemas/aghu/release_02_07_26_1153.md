**Sistema:** aghu
**Data:** 2026-07-02
**Arquivo:** sistemas/aghu/criar_usuario_aghu.py
**O que mudou:**
- `criar_usuario_aghu.py` adicionou `_normalizar_nome_completo` para colapsar sequencias de espacos, tabs e quebras de linha antes de remover espacos laterais do nome completo.
- `_normalizar_usuario_importacao` passou a preencher `UsuarioImportacao.nome_completo` usando `_normalizar_nome_completo`, em vez de apenas aplicar `strip()` ao valor textual.
- `_preparar_usuario_importacao` passou a validar um `UsuarioImportacao` intermediario que preserva `login` e `email` originais, mas usa `nome_completo` normalizado, permitindo aceitar nomes com espacos corrigiveis sem relaxar a validacao de espacos indevidos em login e e-mail.
**Observacoes:** Release criada com o intuito de resolver o problema : Ao iniciar automação em lote, no relatório de saída era exibido para vários usuários "Ignorado : Nome com espaços". Ocorre que não haviam espaços. Problema resolvido.
