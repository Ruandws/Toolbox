**Sistema:** aghu
**Data:** 2026-06-26
**Arquivo:** testes/aghu/test_unit_cadastro_pessoa_aghu.py; testes/aghu/test_regression_cadastro_pessoa_aghu.py; testes/aghu/test_unit_ui_criar_pessoa_aghu.py
**O que mudou:**
- Adicionado `test_unit_cadastro_pessoa_aghu.py` com 34 testes unitarios cobrindo: normalizacao (`_valor_em_branco`, `normalizar_texto`, `texto_planilha`, `apenas_digitos`, `cpf_confere`, `normalizar_entrada`), validacao (`validar_entrada`, `resultado_ignorado`), imutabilidade dos dataclasses (`CadastroPessoaEntrada`, `ResultadoCadastroPessoa`), leitura de planilha (`ler_planilha_cadastros` com aliases, cabecalhos com espacos, extensao invalida, colunas ausentes), relatorios e logs (`salvar_relatorio_resultados`, `gerar_csv_logs`, `controle_saida_terminal`), locators puros (`linha_vazia_visivel`, `_linha_por_cpf`), mensagens do sistema (`mensagens_sistema`, `aguardar_mensagem_gravacao`), `PessoaFlow` (`_normalizar_sexo`, `processar` para cenarios de criacao, atualizacao, pesquisa indefinida e erro de gravacao), maestro (`processar_cadastro`, `processar_cadastros`) e API publica (`executar_cadastro_pessoas`, `executar_cadastro_lote`, `executar_cadastro_individual`).
- Adicionado `test_regression_cadastro_pessoa_aghu.py` com 7 testes de regressao cobrindo: aliases ASCII na planilha, CPF formatado na validacao, pesquisa que nao confirma vazio antes de encontrar CPF, pesquisa indefinida vira `conferir_manual`, propagacao de `url_aghu` no retry do clean state, tentativa unica de clean state em `navegar_ate_cadastro_pessoa`, e `trocar_aba_aghux` fecha page e reautentica na URL informada. Inclui testes de prevalidacao: lote invalido retorna ignorados sem iniciar Playwright e falha tecnica definitiva mantem CPF e nome normalizados.
- Adicionado `test_unit_ui_criar_pessoa_aghu.py` com 29 testes unitarios cobrindo: `obter_url_ambiente_aghu` com ambientes conhecidos e desconhecido, `caminho_relatorio_padrao` com e sem base, alternancia de frames por tipo de execucao, visual do segmented button de sexo, alerta de producao, modal de ambiente, validacao de opcoes de visibilidade, `_credenciais_e_url` com dados validos e invalidos, `_cadastro_individual` com campos e sexo invalido, selecao de planilha e relatorio com cancelamento e preenchimento automatico, bloqueio e liberacao de execucao, despacho por tipo, guarda `em_execucao`, inicio de thread individual e lote com validacoes de extensao e planilha obrigatoria, `_resumir_resultados`, threads de execucao individual e lote com sucesso, ignorado e excecao, e `_finalizar_execucao`.
**Observacoes:** Ruff aprovado sem alteracoes. Todos os 98 testes (34 unitarios + 7 regressao de `cadastro_pessoa_aghu`; 29 unitarios de `ui_criar_pessoa_aghu`; 28 dos demais)
