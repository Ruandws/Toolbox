**Sistema:** aghu
**Data:** 2026-07-27
**Arquivo:** sistemas/aghu/Habilitar_impressora_em_computador/PrinterAGHU.py
**O que mudou:**
- Função `_aguardar_estado_resultado_pesquisa` removida; sua lógica de polling foi absorvida por `_coletar_linhas_computador`.
- Extraída função `_extrair_registro_linha` para encapsular a leitura e montagem de um único registro da tabela, retornando `None` quando a linha não está visível.
- `_coletar_linhas_computador` refatorada: agora realiza o polling diretamente, filtra por IP na própria iteração, retorna assim que encontra registros correspondentes ao IP alvo e distingue timeout sem linhas (`"indefinido"`) de timeout com linhas mas sem correspondência de IP (`"linhas"` com lista vazia) via flag `chegou_a_ver_linhas`.
**Observações:** —
