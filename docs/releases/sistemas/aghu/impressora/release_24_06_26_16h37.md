**Sistema:** aghu
**Data:** 2026-06-24
**Arquivo:** sistemas/aghu/Habilitar_impressora_em_computador/PrinterAGHU.py
**O que mudou:**
- Adicionadas as dataclasses imutaveis `DadosLinhaPlanilha`, `ResultadoLinha` e `AcaoVinculo`, alem da excecao `FalhaTecnicaProcessamento`, para concentrar dados da linha, resultado e decisao de vinculo antes mantidos em variaveis soltas dentro de `processar_computadores`.
- Adicionadas as constantes `MAX_TENTATIVAS_PROCESSAMENTO_LINHA` e `MAX_TENTATIVAS_ESTOQUE`, substituindo os limites fixos usados diretamente nos loops de processamento e cadastro de impressora inexistente.
- Extraidos os helpers `_extrair_dados_linha_planilha`, `_numero_linha_planilha`, `_criar_log_linha`, `_registrar_linha_ignorada` e `_gerar_csv_logs`, removendo a montagem inline dos dados de planilha, logs e arquivo CSV dentro de `processar_computadores`.
- Extraido o fluxo de selecao, pesquisa e gravacao de vinculo para `_selecionar_computador_no_formulario`, `_pesquisar_vinculos_computador`, `_selecionar_impressora_no_formulario`, `_selecionar_classe_vinculo`, `_avaliar_pesquisa_vinculos` e `_gravar_formulario_vinculo`, preservando os retornos de erro para pesquisa indefinida, linhas sem IP esperado, tipo de CUPS divergente e gravacao sem resposta conclusiva.
- Separadas as acoes de vinculo em `_registrar_vinculo_mantido`, `_editar_vinculo_existente`, `_incluir_novo_vinculo` e `_executar_acao_vinculo`, substituindo os blocos inline de decisao `mantido`, `alterar` e `incluir`.
- Reorganizado o tratamento de retentativas e falhas em `_processar_linha_aghu`, `_reiniciar_modulo_vinculo`, `_cadastrar_impressora_inexistente`, `_tratar_impressora_inexistente`, `_tratar_erro_funcional`, `_tratar_falha_tecnica` e `_processar_linha_com_retentativas`, mantendo a reinicializacao de aba/modulo e a marcacao de impressora criada apos sucesso do cadastro auxiliar.
**Observacoes:** Testes especificos `pytest testes/aghu/test_unit_printer_aghu.py testes/aghu/test_regression_printer_aghu.py` aprovados com 60 testes. `ruff check` aprovado.
