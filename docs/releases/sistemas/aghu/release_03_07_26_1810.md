**Sistema:** aghu
**Data:** 2026-07-03
**Arquivo:** sistemas/aghu/criar_pessoa_aghu.py; sistemas/aghu/ui_criar_pessoa_aghu.py; testes/aghu/test_unit_ui_criar_pessoa_aghu.py

**O que mudou:**
- `criar_pessoa_aghu.py` adicionou a dataclass `CampoPessoaSpec` para concentrar nome interno, label da UI, placeholder, aliases de planilha, obrigatoriedade e tipo de controle visual de cada campo de pessoa.
- `criar_pessoa_aghu.py` adicionou `CAMPOS_PESSOA_SCHEMA` como fonte unica dos campos de cadastro de pessoa.
- `ALIASES_COLUNAS` deixou de manter aliases duplicados manualmente e passou a ser derivado de `CAMPOS_PESSOA_SCHEMA`.
- `CAMPOS_PESSOA_OBRIGATORIOS` deixou de ser uma tupla fixa e passou a ser derivado dos campos marcados com `obrigatorio=True` em `CAMPOS_PESSOA_SCHEMA`.
- `criar_pessoa_aghu.py` adicionou `CAMPOS_PESSOA_UI`, derivado do schema com `(nome, label, placeholder, tipo_ui)`, e exportou esse simbolo em `__all__`.
- `ui_criar_pessoa_aghu.py` removeu o `CAMPOS_PESSOA` local e passou a importar `CAMPOS_PESSOA_UI` de `criar_pessoa_aghu.py`.
- `CAMPOS_PESSOA_ESQUERDA` e `CAMPOS_PESSOA_DIREITA` passaram a ser calculados a partir de `CAMPOS_PESSOA_UI`.
- `_criar_linhas_campos_pessoa` passou a receber tuplas com quatro itens e a decidir o seletor de sexo por `tipo_ui == "sexo"`.
- `test_unit_ui_criar_pessoa_aghu.py` atualizou a fixture `app_fake` para consumir `ui.CAMPOS_PESSOA_UI` e desempacotar o novo `tipo_ui`, corrigindo a quebra causada pela remocao de `ui.CAMPOS_PESSOA`.

**Observacoes:** Validado com testes unitarios/regressao especificos de criacao de pessoa AGHU, teste unitario da UI e Ruff.
