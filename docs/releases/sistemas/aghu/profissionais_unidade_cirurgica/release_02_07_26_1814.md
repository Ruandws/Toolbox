**Sistema:** aghu
**Data:** 2026-07-02
**Arquivo:** sistemas/aghu/profissionais_unidade_cirurgica_aghu.py; sistemas/aghu/ui_profissionais_unidade_cirurgica.py; testes/aghu/test_unit_profissionais_unidade_cirurgica_aghu.py

**O que mudou:**
- `profissionais_unidade_cirurgica_aghu.py` adicionou `CadastroProfissionalUnidadeEntrada`, `ResultadoCadastroProfissional`, constantes de status e listas permitidas de unidade/função para representar o cadastro de profissionais da Unidade Cirúrgica.
- `profissionais_unidade_cirurgica_aghu.py` adicionou normalização e validação de entradas com `normalizar_entrada`, `validar_entrada`, aliases de colunas e `ler_planilha_cadastros` para carregar lotes `.xlsx`.
- `profissionais_unidade_cirurgica_aghu.py` adicionou persistência de resultados com `salvar_relatorio_resultados` e `gerar_csv_logs`, incluindo colunas padronizadas de profissional, unidade, função, status e detalhes.
- `profissionais_unidade_cirurgica_aghu.py` adicionou o fluxo Playwright `ProfissionalUnidadeCirurgicaFlow` com pesquisa de vínculo existente, preenchimento por autocomplete/select, gravação, leitura de mensagens do sistema e retorno para a tela de pesquisa.
- `profissionais_unidade_cirurgica_aghu.py` adicionou orquestração de login, navegação, retry com estado limpo, execução individual e execução em lote por `executar_cadastros_profissionais`, `executar_cadastro_individual` e `executar_cadastro_lote`.
- `ui_profissionais_unidade_cirurgica.py` adicionou a interface CustomTkinter `AghuProfissionaisUnidadeCirurgicaApp` com seleção de ambiente, execução unitária/lote, campos de planilha e relatório, bloqueio de controles durante a execução e resumo de resultados.
- `test_unit_profissionais_unidade_cirurgica_aghu.py` adicionou fakes unitários para `_estado_vinculo`, mensagens de gravação, transições de `ProfissionalUnidadeCirurgicaFlow.processar` e retry de `processar_cadastros` após limpeza de estado.

**Observações:** Há duas necessidades pendentes a serem corrigidas posteriormente: o lote ainda pode exibir cor verde quando houver apenas `funcionario_nao_encontrado` ou `ignorado`, e a confirmação da pesquisa ainda pode classificar resultados visíveis após curta estabilidade quando o carregamento não é detectado.
