**Sistema:** aghu
**Data:** 2026-07-08
**Arquivo:** sistemas/aghu/ui_profissionais_unidade_cirurgica.py

**O que mudou:**
- `_bloquear_execucao` e `_liberar_execucao` foram ajustados para iterar sobre `self.checkboxes_unidades_funcionais` ao invés de opções únicas, atualizando corretamente o estado das linhas de lote.
- `iniciar_execucao_individual` foi alterado para instanciar múltiplos cadastros unitários simultâneos chamando `self._cadastros_individuais()`.
- A inicialização do Playwright e do browser foi abstraída no novo método `_executar_com_playwright`, que centraliza a criação e encerramento de context e page.
- `_executar_individual_thread` foi atualizado para processar uma lista de cadastros (`cadastros`), delegando a execução para `executar_cadastros_profissionais` via `_executar_com_playwright`.
- `_executar_lote_thread` foi atualizado para utilizar o `_executar_com_playwright` ao acionar `executar_cadastro_lote`.
- A lógica de cores do resumo de resultados foi corrigida em `_executar_lote_thread` para que os status `STATUS_FUNCIONARIO_NAO_ENCONTRADO` e `STATUS_IGNORADO` resultem na cor amarela (aviso) se não houverem erros, resolvendo a pendência técnica.
- `_resumir_resultados` recebeu os parâmetros opcionais `prefixo` e `rotulo_total` para poder ser reutilizado nos resumos de execução unitária.

**Observações:** —
