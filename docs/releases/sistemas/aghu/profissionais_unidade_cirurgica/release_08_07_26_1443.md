**Sistema:** aghu
**Data:** 2026-07-08
**Arquivo:** sistemas/aghu/profissionais_unidade_cirurgica_aghu.py

**O que mudou:**
- Funções de execução (`executar_cadastros_profissionais`, `_executar_cadastros_profissionais_com_saida_configurada`, `executar_cadastro_lote`, `executar_cadastro_individual`) foram modificadas para receber instâncias ativas de `context` e `page` como parâmetros de entrada (injeção de dependência).
- A responsabilidade de iniciar e fechar o browser via `sync_playwright` foi completamente removida da camada de negócio e transferida para a camada de interface (UI), melhorando a testabilidade e isolamento.

**Observações:** —
