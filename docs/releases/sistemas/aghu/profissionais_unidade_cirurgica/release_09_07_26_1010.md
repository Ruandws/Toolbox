**Sistema:** aghu
**Data:** 2026-07-09
**Arquivo:** sistemas/aghu/ui_profissionais_unidade_cirurgica.py

**O que mudou:**
- `_executar_com_playwright` agora usa `slow_mo=500 if mostrar_browser else 0` em vez de `slow_mo=500` fixo, eliminando o atraso artificial de 500ms por ação do Playwright quando a execução roda em modo headless (`mostrar_browser=False`).
- `test_executar_com_playwright_cria_contexto_e_fecha_browser` atualizado para esperar `"slow_mo": 0` no cenário headless; adicionado `test_executar_com_playwright_usa_slow_mo_com_navegador_visivel` cobrindo o cenário com navegador visível (`slow_mo=500` mantido).

**Observações:** Mudança de performance replicada de forma idêntica em `criar_usuario_aghu.py`, `criar_pessoa_aghu.py`, `concessor_aghu.py` e `ui_alignprinterAGHU.py` — todas as automações AGHU usavam o mesmo `slow_mo=500` fixo, sem justificativa técnica registrada além de depuração visual.
