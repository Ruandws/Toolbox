**Sistema:** aghu
**Data:** 2026-07-06
**Arquivo:** sistemas/aghu/criar_pessoa_aghu.py

**O que mudou:**
- `criar_pessoa_aghu.py` adicionou a constante `ORGAO_EMISSOR_PADRAO` com o valor `SSP - Secretaria de Segurança Pública`.
- `CAMPOS_PESSOA_SCHEMA` removeu o `CampoPessoaSpec` de `orgao_emissor`, retirando `Órgão Emissor` dos aliases de planilha e dos campos obrigatorios derivados.
- `CadastroPessoaEntrada.orgao_emissor` passou a usar `ORGAO_EMISSOR_PADRAO` como valor padrao.
- `normalizar_entrada` passou a sobrescrever `orgao_emissor` com `ORGAO_EMISSOR_PADRAO`.
- `PessoaFlow._preencher_formulario` passou a preencher `SELECTOR_ORGAO` com `ORGAO_EMISSOR_PADRAO`, em vez de usar `entrada.orgao_emissor`.
- `__all__` passou a exportar `ORGAO_EMISSOR_PADRAO`.

**Observacoes:** Validado com testes unitarios/regressao especificos de criacao de pessoa AGHU e Ruff.
