**Sistema:** AGHU
**Data:** 2026-07-07
**Arquivo:** sistemas/aghu/ui_criar_pessoa_aghu.py e testes/aghu/test_unit_ui_criar_pessoa_aghu.py
**O que mudou:**
- Adição de bloqueio e liberação explícitos via `configure(state="disabled")` e `state="normal"` para os campos de entrada `entry_usuario_rede`, `entry_senha`, `entry_planilha_lote` e `entry_relatorio_lote` nos métodos `_bloquear_execucao` e `_liberar_execucao`.
- Correção de erro de indentação (`IndentationError`) que desconfigurava os métodos `_bloquear_execucao` e `_liberar_execucao` e impedia a inicialização da aplicação.
- Atualização da classe mock `FakeEntry` nos testes unitários para herdar de `FakeWidget`, habilitando suporte ao método `configure` e evitando erros de atributo (`AttributeError`).
- Remoção do preenchimento fixo do campo descontinuado `orgao_emissor` da inicialização da estrutura `CadastroPessoaEntrada` nos testes.
**Observações:** —
