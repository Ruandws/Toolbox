**Sistema:** aghu
**Data:** 2026-06-26
**Arquivo:** sistemas/aghu/ui_criar_pessoa_aghu.py; testes/aghu/fixtures_cadastro_pessoa.py; testes/aghu/test_unit_cadastro_pessoa_aghu.py; testes/aghu/test_regression_cadastro_pessoa_aghu.py; testes/aghu/test_unit_ui_criar_pessoa_aghu.py
**O que mudou:**
- Em `AghuCadastroPessoaApp._credenciais_e_url`, adicionado comentario explicando que a senha e obtida sem `.strip()` porque espacos podem ser significativos.
- Em `AghuCadastroPessoaApp._cadastro_individual`, removido o segundo `return CadastroPessoaEntrada(**dados)` que estava inalcancavel.
- Criado `testes/aghu/fixtures_cadastro_pessoa.py` com a factory compartilhada `pessoa_valida()` e os fakes `PageFake`/`ContextFake` usados pelos testes de cadastro de pessoa.
- `test_unit_cadastro_pessoa_aghu.py` e `test_regression_cadastro_pessoa_aghu.py` passaram a importar `pessoa_valida()` e fakes do modulo compartilhado, removendo definicoes duplicadas locais.
- Em `test_unit_ui_criar_pessoa_aghu.py`, adicionada fixture `limpar_threads_fake` com `autouse=True` para limpar `FakeThread.criadas` antes dos testes, substituindo limpezas manuais repetidas.
- Em `test_unit_ui_criar_pessoa_aghu.py`, adicionado teste que documenta a aceitacao de senha composta apenas por espacos em `_credenciais_e_url`.
**Observacoes:** Testes relevantes aprovados com 99 casos. Ruff aprovado. Auditoria registrou apenas um achado baixo de manutencao sobre aliases duplos de nomes de fakes no modulo compartilhado.
