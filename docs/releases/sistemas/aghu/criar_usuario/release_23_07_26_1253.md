**Sistema:** AGHU
**Data:** 2026-07-23
**Arquivo:** `sistemas/aghu/criar_usuario_aghu.py`
**O que mudou:**
- Extração da lógica de validação de usuários para as novas estruturas `_ValidacaoLoteUsuarios` e `_validar_lote_usuarios`.
- Remoção da chamada de validação (`_preparar_usuario_importacao`) de dentro dos laços em `processar_usuarios` e `importar_usuario`.
- Criação do método `_combinar_resultados_na_ordem_original` para garantir que a saída final mantenha a ordem original da planilha após separar itens válidos de ignorados.
- Centralização da chamada de validação do lote de uma vez só em `executar_importacao_usuarios` antes da inicialização do Playwright, abortando precocemente caso nenhum usuário seja válido.
**Observações:** Esta refatoração isola a etapa de validação, permitindo que a checagem dos dados ocorra integralmente de forma síncrona antes do engajamento do workflow de automação via browser.
