**Sistema:** aghu
**Data:** 2026-06-25
**Arquivo:** sistemas/aghu/ui_criar_usuario_aghu.py
**O que mudou:**
- Adicionada a variável `self.var_browser` e o respectivo CheckBox `self.checkbox_browser` para controle de exibição do navegador.
- Adicionados os métodos `_validar_opcoes_visibilidade_do_browser` e `_validar_opcoes_visibilidade_do_console` para impedir que navegador e console fiquem invisíveis simultaneamente.
- Atualizados os métodos `_bloquear_execucao` e `_liberar_execucao` para desabilitar/habilitar a interação com `self.checkbox_browser`.
- Modificados `iniciar_execucao_individual` e `iniciar_execucao_lote` para extrair o valor de `mostrar_browser` e repassá-lo para as threads de execução.
- Atualizados `_executar_individual_thread` e `_executar_lote_thread` para receber o parâmetro `mostrar_browser` em vez de utilizar o valor `True` por padrão (hardcoded).
**Observações:** Identificada redundância na lógica de validação de visibilidade, sugerida unificação. Ruff validado sem alterações e todos os testes passaram com sucesso.
