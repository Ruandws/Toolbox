**Sistema:** aghu
**Data:** 2026-07-09
**Arquivo:** sistemas/aghu/ui_criar_usuario_aghu.py e testes/aghu/test_unit_ui_criar_usuario_aghu.py
**O que mudou:**
- Novo método `_usuarios_individuais_incompletos`, chamado em `iniciar_execucao_individual` antes de criar a thread: se algum usuário informado tiver login, nome completo ou e-mail em branco, a execução é bloqueada com `ValueError` e status vermelho, sem chegar a abrir o navegador.
- `iniciar_execucao_lote` passou a validar a extensão `.xlsx` do relatório de saída (`caminho_relatorio`) antes de iniciar a thread, no mesmo padrão já usado para a planilha de entrada.
- Novo método `_cor_resultado`: vermelho quando há `STATUS_ERRO` no lote/unitário, laranja quando há `STATUS_IGNORADO` ou `STATUS_CONFERIR_MANUALMENTE` sem erro, verde nos demais casos. Substitui o `"green"` fixo em `_executar_individual_thread` e `_executar_lote_thread`.
- `_resumir_resultados` passou a listar "Conferir manualmente: N." como categoria própria no resumo de lote/unitário.
- Testes novos: `test_iniciar_execucao_individual_mostra_erro_com_campo_em_branco`, `test_iniciar_execucao_lote_valida_extensao_xlsx_do_relatorio`, e a classe `TestCorResultado` (4 casos cobrindo verde/laranja/vermelho). Testes existentes (`test_resumir_resultados_conta_status_conhecidos`, `test_executar_individual_thread_resume_multiplos_resultados`, `test_executar_lote_thread_agenda_finalizacao_com_resumo`) atualizados para o novo formato de resumo e para a cor `"red"` quando há erro no lote unitário com múltiplos resultados.
**Observações:** Ruff e os 46 testes de `test_unit_ui_criar_usuario_aghu.py` aprovados sem alterações adicionais.
