**Sistema:** aghu
**Data:** 2026-07-03
**Arquivo:** sistemas/aghu/ui_criar_usuario_aghu.py

**O que mudou:**
- `ui_criar_usuario_aghu.py` substituiu os campos unitarios fixos `entry_login_individual`, `entry_nome_individual` e `entry_email_individual` por uma lista dinamica em `linhas_usuarios_individual`.
- `ui_criar_usuario_aghu.py` adicionou `MAX_USUARIOS_MANUAIS = 5` e os metodos `adicionar_linha_usuario`, `remover_linha_usuario`, `coletar_usuarios_individuais` e `_atualizar_estado_lista_usuarios`.
- `_criar_campos_individual` passou a renderizar cabecalho, linhas dinamicas, botao de adicionar usuario e mensagem de limite atingido.
- `iniciar_execucao_individual` passou a coletar uma lista de `UsuarioImportacao` e encaminha-la para `_executar_individual_thread`, em vez de enviar tres strings soltas.
- `_executar_individual_thread` deixou de chamar `executar_importacao_individual` e passou a chamar `executar_importacao_usuarios`; quando ha mais de um resultado, usa `_resumir_resultados` com o prefixo `Execucao unitaria concluida`.
- `_bloquear_execucao` e `_liberar_execucao` passaram a atualizar o estado dos campos, botao de adicionar e botoes de remover da lista dinamica durante a execucao.
- `test_unit_ui_criar_usuario_aghu.py` foi atualizado para cobrir adicao, limite, remocao, reindexacao, coleta ordenada, bloqueio/liberacao e despacho da thread com multiplos usuarios.

**Observacoes:** Validado com testes unitarios/regressao especificos de criacao de usuario AGHU e Ruff.
