**Sistema:** aghu
**Data:** 2026-07-02
**Arquivo:** sistemas/aghu/concessor_aghu.py; sistemas/aghu/ui_concessor.py

**O que mudou:**
- `concessor_aghu.py` adicionou `ALIASES_COLUNAS_PLANILHA`, `CAMPOS_CONCESSAO_OBRIGATORIOS`, `_normalizar_cabecalho_planilha`, `_mapear_colunas_planilha` e `ler_planilha_concessoes` para carregar concessões por planilha `.xlsx` com aliases normalizados de login, protocolo, escopo e categoria.
- `concessor_aghu.py` adicionou `resultado_ignorado`, `_preparar_concessoes_lote`, `executar_concessoes_perfis` e `executar_concessao_lote` para pré-validar linhas, ignorar entradas inválidas, evitar Playwright quando não houver perfis automatizáveis e salvar relatório consolidado.
- `concessor_aghu.py` adicionou `processar_concessoes` para reutilizar a sessão autenticada e processar uma lista de `ConcessaoPreparada` no mesmo fluxo de navegador.
- `ui_concessor.py` adicionou `TIPO_INDIVIDUAL`, `TIPO_LOTE`, `caminho_relatorio_padrao`, seletor segmentado de tipo de execução, campos de planilha/relatório e seletores de arquivo para concessão em lote.
- `ui_concessor.py` alterou `iniciar_execucao` para despachar entre fluxo unitário e lote, adicionou `iniciar_execucao_lote`, `_executar_lote_thread` e `_resumir_resultados`, e passou a bloquear/liberar também os controles de lote durante a execução.

**Observações:** Achados conhecidos: o lote pode reutilizar `page`/`janela_sistema` obsoletos após retry ou troca de aba em uma concessão; a UI marca o lote em verde mesmo quando houver `conferir_manual`; e a cobertura automatizada ainda não exercita lote misto com retry entre linhas. Esses pontos serão mantidos por ora.
