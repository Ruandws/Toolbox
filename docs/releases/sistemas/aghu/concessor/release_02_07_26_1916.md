**Sistema:** aghu
**Data:** 2026-07-02
**Arquivo:** sistemas/aghu/concessor_aghu.py

**O que mudou:**
- `concessor_aghu.py` adicionou aliases em letra minuscula ao campo `login` em `ALIASES_COLUNAS_PLANILHA`: `login`, `usuario`, `usuario alvo`, `usuario aghu` e `login do usuario`.
- `concessor_aghu.py` adicionou aliases em letra minuscula ao campo `protocolo` em `ALIASES_COLUNAS_PLANILHA`: `protocolo`, `nro protocolo`, `numero protocolo`, `numero do protocolo`, `chamado` e `despacho`.
- `concessor_aghu.py` adicionou aliases em letra minuscula aos campos `escopo` e `categoria`, incluindo `escopo`, `categoria`, `perfil` e `grupo`.

**Observacoes:** A leitura de cabecalhos de planilha ja normaliza aliases com `casefold()` em `_normalizar_cabecalho_planilha`, portanto as novas entradas minusculas documentam explicitamente as variacoes aceitas, mas nao ampliam o comportamento efetivo de correspondencia.
