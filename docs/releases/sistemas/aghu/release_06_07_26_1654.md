**Sistema:** aghu
**Data:** 2026-07-06
**Arquivo:** sistemas/aghu/autenticador.py

**O que mudou:**
- `autenticador.py` adicionou a constante `TIMEOUT_RESULTADO_AUTENTICADOR_MS` com limite fixo de 10000 ms para a confirmacao do resultado apos o clique em `Entrar`.
- `autenticar_aghu_page` passou a usar `TIMEOUT_RESULTADO_AUTENTICADOR_MS` no prazo final de verificacao de sucesso, credenciais invalidas ou timeout apos submeter o login, em vez de reutilizar `timeout_ms`.
- A mensagem de timeout final de `autenticar_aghu_page` passou a informar explicitamente o limite fixo de 10s quando o login nao e confirmado.

**Observacoes:** Validado com testes unitarios/regressao/UI especificos de autenticador AGHU e Ruff.
