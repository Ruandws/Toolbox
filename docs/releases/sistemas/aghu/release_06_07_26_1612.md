**Sistema:** aghu
**Data:** 2026-07-06
**Arquivo:** sistemas/aghu/criar_usuario_aghu.py

**O que mudou:**
- `criar_usuario_aghu.py` adicionou a constante `TEMPO_MAXIMO_CONSULTA_USUARIO_MS` com timeout de 13000 ms para a espera de mensagens na gravacao de usuario.
- `criar_usuario_aghu.py` adicionou `_titulo_widget_carregamento` para reconhecer o indicador de carregamento pelo titulo `Carregando`, alem do widget `Aguarde...`.
- `_existe_carregamento_visivel` passou a iterar por `_widgets_carregamento`, protegendo a criacao e avaliacao de cada locator de carregamento separadamente.
- `_aguardar_carregamento_sumir` passou a centralizar a espera do carregamento com `wait_for(state="visible")` e `wait_for(state="hidden")`, substituindo o loop manual usado na consulta.
- `_aguardar_ciclo_carregamento_consulta` passou a delegar a espera de carregamento para `_aguardar_carregamento_sumir` e manteve erro por timeout quando o carregamento nao some.
- `_aguardar_mensagem_gravacao` passou a aguardar o carregamento sumir antes de consultar mensagens de sucesso, duplicidade ou erro.
- `test_regression_criar_usuario_aghu.py` atualizou `WidgetCarregamentoFake` para simular `wait_for(state="hidden")`.
- `test_regression_criar_usuario_aghu.py` adicionou regressao para validar que `_aguardar_mensagem_gravacao` aguarda o carregamento antes de ler a mensagem de sucesso.

**Observacoes:** Validado com testes unitarios/regressao especificos de criacao de usuario AGHU e Ruff.
