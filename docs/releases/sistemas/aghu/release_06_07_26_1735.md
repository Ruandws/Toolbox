**Sistema:** aghu
**Data:** 2026-07-06
**Arquivos:**
- `sistemas/aghu/criar_usuario_aghu.py`
- `testes/aghu/test_regression_criar_usuario_aghu.py`

**O que mudou:**
- `criar_usuario_aghu.py` passou a importar `expect` do Playwright e adicionou `ERROS_ESPERA_PLAYWRIGHT` para centralizar as excecoes tratadas nas esperas de locators.
- Foi adicionado `TEMPO_CHECAGEM_IMEDIATA_MS = 1` para impedir chamadas de checagem rapida com `timeout=0`, evitando que uma assertion do Playwright fique sem limite de timeout.
- Foram adicionadas as funcoes `_locator_visivel`, `_locator_oculto`, `_primeiro_entre_locators`, `_linha_tabela_por_texto`, `_linha_tabela_vazia` e `_linha_tabela_com_dados` para substituir verificacoes manuais de visibilidade, linhas vazias e combinacao de resultados.
- `_linha_tabela_por_login` deixou de iterar linha a linha com `count()`, `nth()` e leitura de `inner_text`; agora localiza a linha por seletor da tabela, coluna de login e filtro `has` com regex de login exato.
- `_aguardar_carregamento_sumir` passou a reutilizar `_locator_visivel` e `_locator_oculto`, removendo blocos repetidos de `try/except` para aguardar exibicao e ocultacao dos widgets de carregamento.
- `_aguardar_resultado_pesquisa_usuario` e `_aguardar_resultado_identity` deixaram de usar loops com `time.sleep`, estado pendente e `_pode_confirmar_resultado_sem_carregamento`; agora aguardam o primeiro locator visivel entre login, linha vazia e linha com dados, mantendo a checagem curta de estabilidade antes de confirmar ausencia de login.
- Foi corrigido o caso em que a pesquisa inicial retornava `Nenhum registro encontrado!`, mas o fluxo nao prosseguia para `Importar Usuario` por causa de uma checagem posterior de login com timeout zero.
- `_aguardar_mensagem_gravacao` passou a aguardar o carregamento sumir antes de combinar mensagens de sucesso, duplicidade e erro com `_primeiro_entre_locators`, removendo polling manual sobre `count()`, `first.is_visible()` e `time.sleep`.
- `test_regression_criar_usuario_aghu.py` recebeu coberturas para:
  - checagem imediata sem `timeout=0`;
  - pesquisa inicial com tabela vazia retornando `nao_encontrado`;
  - abertura da importacao quando a pesquisa inicial nao encontra registros;
  - manutencao da janela curta de estabilidade antes de confirmar ausencia de login.

**Validacoes executadas:**
- `python -m py_compile sistemas/aghu/criar_usuario_aghu.py testes/aghu/test_regression_criar_usuario_aghu.py`
- `pytest testes/aghu/test_unit_criar_usuario_aghu.py testes/aghu/test_regression_criar_usuario_aghu.py testes/aghu/test_unit_ui_criar_usuario_aghu.py`
- Resultado dos testes especificos de criacao de usuario AGHU: `93 passed`.

**Observacoes:**
- Ruff nao foi executado nesta validacao.
- A execucao ampla de `pytest testes/aghu` passou em `359` testes e falhou em `1` teste fora deste escopo, em `test_unit_concessor_aghu.py`, relacionado a alias de planilha do `concessor_aghu.py`.
